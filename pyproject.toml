[project]
name = "adobe-creative-provenance-gate"
version = "0.1.0"
description = "Deterministic content-addressed creative provenance and lineage verifier"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
